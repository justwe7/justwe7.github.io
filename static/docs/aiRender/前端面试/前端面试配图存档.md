# 前端面试配图存档

生成日期：2026-07-16

适用文章：

- `docs/前端面试/Node.js面试知识整理.md`
- `docs/前端面试/React面试知识整理.md`
- `docs/前端面试/Vue3面试知识整理（Vue2开发者过渡版）.md`
- `docs/前端面试/Vue面试知识整理.md`
- `docs/前端面试/Webpack面试知识整理.md`

本次只处理文章中已有的 `[!VISUALIZATION]` 标记，不额外选择插图位置。所有正文配图均为 16:9 横向 WebP，引用路径使用 `../../static/docs/aiRender/前端面试/...`。

## 共用风格提示词

Minimalist bold-line hand-drawn editorial illustration, strong confident black ink contours with expressive variation in line weight, slightly organic hand-drawn edges, sparse thin interior detail lines, every object described with as few lines as possible, large areas of clean negative space, clean white background by default, mostly monochrome black ink with one small warm orange accent color only, crisp readable silhouettes, clear information hierarchy, no watercolor wash, no pencil shading, no hatching, no heavy paper grain, no complex texture, no watermark, no border.

Use the provided wuxia mascot image as the strict character reference for body shape, hairstyle, clothing and color palette. Preserve the chibi Chinese young swordsman identity: large head and small agile body, near-black dark burgundy long hair, asymmetric long bangs covering one eye, high back-tied hair with dark blue ribbon, one visible narrow gray-blue eye, two dark cinnabar diagonal marks on the visible cheek, deep indigo navy and gray-blue Chinese wuxia outfit, and one Chinese short sword with a brown-gold tassel. Keep the article anchor and information structure more important than the character pose.

## 插图说明清单与构思摘要

| 文件 | 标题 | 图型 | 单一命题 | 插图说明 | QA 备注 |
|---|---|---|---|---|---|
| `nodejs-01-runtime-layers.webp` | Node.js 运行时分层 | architecture | Node.js 异步能力来自 JS 主线程、V8、绑定层、libuv、OS 和线程池的分工，不是“只有一个线程”。 | 分层展示 JS、V8、绑定层、libuv、OS 与线程池侧路，回调回到事件循环。 | 可用 |
| `nodejs-02-stream-backpressure.webp` | Stream 管道与背压 | flow | Stream 是分块流动和压力反馈，不是一次性读完整文件。 | Readable、Transform、Writable 后接缓冲水位，橙色背压箭头回传。 | 可用 |
| `nodejs-03-commonjs-resolve-cache.webp` | CommonJS 模块解析与缓存 | flow | `require()` 会先解析到唯一文件名，再执行并缓存，不是每次重新执行。 | 从当前模块开始查找内置、相对路径、逐级 `node_modules`，最后进入缓存。 | 生成了第二个侠客，建议后续重出 |
| `nodejs-04-event-loop-microtasks.webp` | Node.js 事件循环与微任务检查点 | flow | libuv 阶段和 JS 回调后的微任务检查点是两层机制。 | timers、poll、check 环路旁放置 nextTick/Promise 微任务队列。 | 可用 |
| `nodejs-05-koa-onion.webp` | Koa 洋葱模型 | flow | `await next()` 是进入下游和恢复上游的分界，执行顺序先进后出。 | 请求进入嵌套中间件，响应沿橙色箭头反向返回。 | 可用 |
| `nodejs-06-jwt-verify.webp` | JWT 三段结构与验签 | relationship | JWT 验签只证明 Header/Payload 未被篡改，之后仍需检查声明。 | Header、Payload、Signature 三段连接到验签印章和 exp/iss/aud 清单。 | 可用 |
| `nodejs-07-performance-diagnosis.webp` | Node.js 性能与内存问题诊断路径 | flow | 性能诊断应先度量，再分流到 CPU、事件循环、堆、外部内存和 I/O，最后回归验证。 | 异常进入度量节点，再分流为多个仪表盘。 | 可用 |
| `react-01-render-commit-effects.webp` | render、commit、浏览器绘制与 Effect 时序 | flow | render、commit、layoutEffect、paint、useEffect 是不同阶段。 | 时间线区分可重试 render、DOM commit、绘制前后 Effect。 | 可用 |
| `react-02-class-lifecycle.webp` | 类组件创建、更新与卸载生命周期 | flow | 现代类生命周期重点在 render 纯净、snapshot 在提交前、didUpdate 在提交后。 | mount/update/unmount 三条路径汇入 commit 门。 | 可用 |
| `react-03-scheduler-fiber-commit.webp` | React 更新：调度、协调与提交 | architecture | Fiber 协调可暂停重试，commit 不可中断，Fiber 不是 DOM。 | update 经 scheduler 进入 Fiber 关系图，最后过 commit 门到 DOM。 | 角色略大但可用 |
| `react-04-transition-priority.webp` | 紧急更新与 Transition 的调度优先级 | flow | Transition 是可中断渲染调度，不是 `setTimeout`。 | input 分成 urgent 快车道与 transition 可暂停/重启车道。 | 可用 |
| `react-05-ssr-hydration.webp` | SSR 到 Hydration 的请求与交互链路 | flow | SSR 先给 HTML，hydration 复用 DOM 并绑定交互。 | Server、HTML、首屏、JS、Hydration、交互依次连接。 | 可用 |
| `vue3-01-options-composition-composable.webp` | Options API、Composition API 与 composable 的逻辑组织方式 | comparison | Composition API 按功能组织和显式复用，不代表 Options API 废弃。 | 左侧 Options 分散货架，右侧 setup/composable 聚合并复用。 | 复用处有侠客缩略图，轻微违约 |
| `vue3-02-reactivity-track-trigger.webp` | Vue 3 响应式的依赖收集与触发流程 | flow | Vue 3 按属性收集依赖，并通过批处理调度更新。 | Proxy 属性 key 被 track 到 dep，trigger 后进入 batch 和 render。 | 可用 |
| `vue3-03-compiler-patchflag.webp` | Vue 3 编译期标记与运行时定向更新 | relationship | 编译期信息帮助运行时定向更新，但不是没有 Diff 或 VDOM。 | template 分离 static/dynamic，dynamic 盖 PatchFlag 后进入 runtime patch。 | 小代码字较多但可用 |
| `vue3-04-programmatic-modal.webp` | Vue 3 程序化 Modal 的挂载、交互与卸载 | architecture | 程序化 Modal 要显式挂载、Teleport、交互、卸载清理。 | show 创建 container/VNode，经 Teleport 到 body，关闭后 cleanup。 | 可用 |
| `vue2-01-instance-first-mount.webp` | Vue 2 实例创建到首屏挂载 | flow | Vue 2 首屏由初始化状态、render Watcher、VNode 和 patch 串起来。 | new Vue 到 data、render、Watcher、VNode、patch、DOM 的流程。 | 可用 |
| `vue2-02-reactive-diff-patch.webp` | Vue 2 响应式更新与 Diff | flow | 多次同步修改进入队列合并，key 帮助同层 Diff 识别节点。 | setter、dep、queue、VNode、key Diff、patch，上下两行 old/new 对比。 | 可用 |
| `vue2-03-keep-alive-lifecycle.webp` | 普通组件与 keep-alive 缓存组件的生命周期 | comparison | keep-alive 离开时缓存而非销毁，回来是 activated 而非重新 mounted。 | normal 左侧 destroy/remount，keep-alive 右侧 cache/deactivated/activated。 | 生成了多个侠客，信息正确但建议后续重出 |
| `webpack-01-build-pipeline.webp` | Webpack 从入口到产物的构建流程 | flow | Webpack 构建是 Compiler/Compilation 驱动的依赖递归构建和产物输出。 | entry、Compiler、Compilation、loader、plugin hook、chunk、emit 串联。 | 偏满但可用 |
| `webpack-02-hmr-chain.webp` | HMR 更新链路 | flow | HMR 是 dev server 与 client runtime 协作，accept 边界决定局部替换或刷新。 | 文件变化、compile、WebSocket、manifest、hot-update、accept、replace/refresh。 | 可用 |
| `webpack-03-prod-optimizations.webp` | 生产构建中代码分割、缓存与 Tree Shaking 的关系 | relationship | Tree Shaking、代码分割和 contenthash 可协作但解决的问题不同。 | 三条分支分别指向 unused、async chunk、cache。 | 角色偏大但可用 |
| `webpack-04-webpack-vite-dev.webp` | Webpack 打包开发与 Vite 原生 ESM 开发的请求差异 | comparison | Vite 开发期按需 ESM 请求，但仍有依赖预构建和生产打包。 | 左侧 Webpack bundle 到 Browser，右侧 Vite ESM/transform/dep prebundle/prod bundle。 | 可用 |

## 生成提示词核心

以下每条提示词均追加了“共用风格提示词”和“固定侠客参考”约束。

1. `nodejs-01-runtime-layers.webp`：layered Node.js runtime stack with business JavaScript, V8, Node.js bindings, libuv, OS, thread-pool side lane, callback path returning to event loop; labels JS, V8, 绑定层, libuv, OS, 线程池, 回调.
2. `nodejs-02-stream-backpressure.webp`：Node.js Stream pipeline of chunk cards moving Readable -> Transform -> Writable, downstream buffer threshold, backpressure signal backward, drain then resume; labels 可读流, 转换流, 可写流, 背压, 排空, 恢复.
3. `nodejs-03-commonjs-resolve-cache.webp`：CommonJS require resolver path from current module, core module, relative path, climbing node_modules, execution and resolved-filename cache; labels require, 内置, 当前模块, node_modules, 执行, 缓存.
4. `nodejs-04-event-loop-microtasks.webp`：Node.js event loop circular track with timers, poll, check and separate microtask checkpoint after callback; labels timers, poll, check, nextTick, Promise, 回调.
5. `nodejs-05-koa-onion.webp`：Koa onion nested middleware rings, request inward and response outward in reverse order, await next() as turning gate; labels 请求, 日志, 异常, 鉴权, 业务, await next, 响应.
6. `nodejs-06-jwt-verify.webp`：JWT three connected segments Header, Payload, Signature, verification seal by key, post-verification claims checklist; labels Header, Payload, Signature, 验签, exp, iss, aud.
7. `nodejs-07-performance-diagnosis.webp`：Node.js diagnosis fork from abnormal latency/throughput through measurement then CPU/event loop/heapUsed/external/RSS/downstream I/O and regression; labels 异常, 度量, CPU, 事件循环, heapUsed, external, RSS, 回归.
8. `react-01-render-commit-effects.webp`：React render-to-effects timeline separating render, commit, layoutEffect, paint, useEffect; labels render, commit, layoutEffect, paint, useEffect.
9. `react-02-class-lifecycle.webp`：React class lifecycle flow split into mount/update/unmount, snapshot before DOM update and didUpdate after commit; labels mount, update, unmount, render, snapshot, commit, didUpdate, cleanup.
10. `react-03-scheduler-fiber-commit.webp`：React Fiber update architecture: update -> scheduler -> interruptible workInProgress Fiber tree with child/sibling/return links -> non-interruptible commit -> DOM; labels update, scheduler, Fiber, child, sibling, return, commit, DOM.
11. `react-04-transition-priority.webp`：React concurrent scheduling timeline with urgent input echo fast lane and interruptible transition list lane pause/restart; labels input, urgent, transition, pause, restart, list.
12. `react-05-ssr-hydration.webp`：SSR to hydration chain: server renders HTML, browser first paint, JS downloads, hydration reuses DOM and binds events, interaction works; labels Server, HTML, First paint, JS, Hydration, 交互.
13. `vue3-01-options-composition-composable.webp`：Vue logic organization comparison, Options scatters one feature across data/methods/computed/watch while Composition setup/composable groups and reuses it; labels Options, data, methods, computed, setup, composable, 复用.
14. `vue3-02-reactivity-track-trigger.webp`：Vue 3 dependency tracking: proxy object property keys, effect reader, dep map, mutation, scheduler batch, rerender; labels Proxy, key, track, dep, trigger, batch, render.
15. `vue3-03-compiler-patchflag.webp`：Vue 3 compiler-to-runtime update relation: template static/dynamic nodes, PatchFlag stamp, dynamic children, runtime patch; labels template, static, dynamic, PatchFlag, runtime, patch.
16. `vue3-04-programmatic-modal.webp`：Vue 3 programmatic modal lifecycle: show(), container, VNode, Teleport to body, callbacks/loading, unmount and cleanup; labels show, container, VNode, Teleport, body, loading, unmount, cleanup.
17. `vue2-01-instance-first-mount.webp`：Vue 2 first mount flow: new Vue, data init, render creation, render Watcher, VNode, patch, DOM; labels new Vue, data, render, Watcher, VNode, patch, DOM.
18. `vue2-02-reactive-diff-patch.webp`：Vue 2 reactive update and Diff pipeline: setter, dep, queue, VNode, keyed old/new Diff, patch; labels setter, dep, queue, VNode, key, Diff, patch.
19. `vue2-03-keep-alive-lifecycle.webp`：Vue keep-alive lifecycle comparison: normal destroy/remount versus cache deactivated/activated without mounted again; labels normal, destroy, remount, keep-alive, cache, deactivated, activated, no mounted.
20. `webpack-01-build-pipeline.webp`：Webpack build pipeline: Compiler creates fresh Compilation, recursive module build, loaders transform, plugins tap hooks, chunks, emit; labels entry, Compiler, Compilation, loader, plugin, chunk, emit.
21. `webpack-02-hmr-chain.webp`：Webpack HMR chain: file change, dev server compile, WebSocket notify, manifest, hot-update chunk, accept boundary, replace or refresh; labels change, compile, WebSocket, manifest, hot-update, accept, replace, refresh.
22. `webpack-03-prod-optimizations.webp`：Webpack production optimization map: Tree Shaking removes unused code, splitting creates async chunk, contenthash supports cache; labels Tree Shaking, split, async chunk, contenthash, cache, unused.
23. `webpack-04-webpack-vite-dev.webp`：Webpack/Vite dev request comparison: Webpack prebuilt bundle versus Vite ESM requests, transform, dep prebundle, production bundle; labels Webpack, bundle, Browser, Vite, ESM, transform, dep prebundle, prod bundle.
