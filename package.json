{
  "name": "justwe7-books",
  "description": "日常的笔记",
  "author": "ilihuaxi@gmail.com",
  "private": true,
  "scripts": {
    "dev": "npx vuepress dev",
    "build": "npx vuepress build",
    "publish": "git pull origin feature && git add . && git commit -m 'update' && git push origin feature"
  },
  "dependencies": {
    "vuepress": "^1.2.0",
    "vuepress-plugin-comment": "^0.7.2"
  },
  "devDependencies": {
    "@vuepress/plugin-back-to-top": "^1.2.0",
    "@vuepress/plugin-nprogress": "^1.2.0",
    "vuepress-auto-sidebar.js": "^1.0.1"
  }
}
